import React from "react";
import {
  Text,
  View,
  ScrollView,
  SectionList,
  TouchableHighlight
} from "react-native";

const Session = ({ data }) => {
  return (
    <View>
      <Text>Sessions</Text>
    </View>
  );
};

export default Session;
